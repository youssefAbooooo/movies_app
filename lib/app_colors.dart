import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0XFF07B5E3);
  // static const Color primaryLight = Color(0xFF8AB4F8);
  // static const Color primaryDark = Color(0xFF1A73E8);

  // Secondary/accent colors
  static const Color secondary = Color(0xFF01D277);

  // Neutral colors
  static const Color background = Color(0xFF121212);
  // static const Color surface = Colors.white;
  // static const Color error = Color(0xFFB00020);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color.fromARGB(255, 117, 117, 117);
  // static const Color textHint = Color(0xFF9AA0A6);

  // Other common colors
  static const Color surface = Color(0xFF1E1E1E);
  //? static const Color details = Color(0XFF07B5E3);
  static const Color divider = Color(0xFF2C2C2C);
  // static const Color disabled = Color(0xFFE8EAED);

  static const Color tmdbFirstColor = Color(0XFF07B5E3);
  static const Color tmdbSecondColor = Color(0XFF8ECEA2);
}
